import JoinTemplate from "@/components/templates/JoinTemplate";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Join Cromachain",
};

export default function JoinPage() {
  return <JoinTemplate />;
}
